

## Plan: Add Favicon

1. Copy `user-uploads://faviconnnnn.png` to `public/favicon.png`
2. Delete `public/favicon.ico` if it exists
3. Add `<link rel="icon" href="/favicon.png" type="image/png">` to `index.html` `<head>`

